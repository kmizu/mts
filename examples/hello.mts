// Hello World example for MTS

let greeting = "Hello, World!";
let double = (x) => x * 2;
let result = double(21);

// Array example
let numbers = [1, 2, 3];
let len = length(numbers);
let first = head(numbers);

// Object example
let person = {
  name: "Alice",
  age: 30,
};

// Show the result
result;
